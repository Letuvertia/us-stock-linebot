# JS deps and build output
node_modules/
dist/
*.js.map

# Python build artifacts
__pycache__/

# Local tooling state (per-machine, never committed)
.claude/
.gitnexus

# Secrets and credentials
.secrets/
.clasprc.json
.env

# Editor / OS scratch
*~
*.un~

# MarketWatch cron resume state (local only)
.mw_progress.json
